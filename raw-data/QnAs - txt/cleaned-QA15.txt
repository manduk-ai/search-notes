Difference between torch.Tensor, torch.FloatTensor, torch.tensor and torch.empty?
torch.Tensor and torch.FloatTensor are depricated
torch.tensor taking data
torch.empty taking size
####

Difference between str() and repr()
str() is used for creating output for end user 
while repr() is mainly used for debugging and development.
----
repr() compute the “official” string representation of an object 
str() is used to compute the “informal” string representation 
----
The print statement and str() built-in function uses __str__ to display 
the string representation of the object while the repr() built-in function 
uses __repr__ to display the object.
####

Which environment variable for Python tells the Python interpreter 
where to locate the module files imported into a program?
PYTHONPATH
####

What function reverses objects of list?
list.reverse()
####

What will be the output of the following code?
~~~~~19
bitwise negation
-20
~number is equivalent to –(x+1).
####

What is the output of the following code?
import math
def main():
  math.cos(math.pi)
print(main())
None is printed because function does not return the value.
####

How to sleep the python code for the desired time?
import time
time.sleep(5)  # 5s
####

What is the output of L[-2] if L = [1,2,3]?
2
